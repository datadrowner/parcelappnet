{
  "config": {
    "step": {
      "user": {
        "title": "ParcelApp Configuration",
        "description": "Enter your ParcelApp API key",
        "data": {
          "api_key": "API Key",
          "poll_interval": "Poll interval (seconds)"
        }
      }
    },
    "error": {
      "invalid_auth": "Invalid API key",
      "cannot_connect": "Failed to connect to ParcelApp API"
    },
    "abort": {
      "already_configured": "ParcelApp is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "ParcelApp Options",
        "description": "Configure ParcelApp settings",
        "data": {
          "poll_interval": "Poll interval (seconds)",
          "filter_mode": "Filter mode (active or recent)"
        }
      }
    }
  }
}
